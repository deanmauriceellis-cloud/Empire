// Empire Reborn — WebSocket Message Protocol
// Re-exported from shared package (canonical source).

export type {
  GamePhase,
  ClientMessage,
  ClientAction,
  ServerMessage,
  VisibleGameState,
  VisibleCity,
} from "@empire/shared";
